<Window x:Class="ICA.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ICA Update"
        Width="340" Height="210"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <Border CornerRadius="12" Padding="20" Margin="8">
        <Border.Background>
            <SolidColorBrush Color="#2D2D30" Opacity="0.97"/>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.4"/>
        </Border.Effect>

        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Обновление ICA"
                       FontSize="16" FontWeight="Bold"
                       Foreground="White" Margin="0,0,0,10"/>

            <TextBlock x:Name="StatusText"
                       Text="Проверка обновлений..."
                       FontSize="13" Foreground="#DDDDDD"
                       TextWrapping="Wrap" LineHeight="20"
                       Margin="0,0,0,14"/>

            <ProgressBar x:Name="Progress"
                         Height="8" Minimum="0" Maximum="100" Value="0"
                         Foreground="#4CAF50" Background="#555555"
                         BorderThickness="0"/>

            <TextBlock x:Name="PercentText"
                       Text=""
                       FontSize="11" Foreground="#999999"
                       HorizontalAlignment="Right" Margin="0,6,0,0"/>
        </StackPanel>
    </Border>
</Window>

